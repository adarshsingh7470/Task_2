pipeline {
  agent any
  stages {
    stage('version') {
      steps {
        sh 'python3 app.py'
      }
    }
    stage('hello') {
      steps {
        sh 'python3 website_tets-.py'
      }
    }
  }
}
